#Para rodar o projeto voce tem que rodar esse comando V

pip install -r requirements.txt

#Ele instala as dependencias do projeto, apos isso, na pasta raiz do projeto voce vai rodar V

python manage.py runserver

